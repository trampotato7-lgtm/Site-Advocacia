backend:
  name: git-gateway
  branch: main

media_folder: "public/images/uploads"
public_folder: "/images/uploads"

collections:
  # Coleção de artigos (salva .md)
  - name: "posts"
    label: "Artigos"
    folder: "public/content/posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "Título", name: "title", widget: "string"}
      - {label: "Data", name: "date", widget: "datetime"}
      - {label: "Categoria", name: "category", widget: "string", required: false}
      - {label: "Descrição", name: "description", widget: "text", required: false}
      - {label: "Imagem de destaque", name: "image", widget: "image", required: false}
      - {label: "Autor", name: "author", widget: "string", default: "Dr. Carlos Silva"}
      - {label: "Conteúdo", name: "body", widget: "markdown"}

  # Coleção que gerencia a lista de artigos (posts.json)
  - name: "postsIndex"
    label: "Lista de Artigos"
    files:
      - name: "postsIndexFile"
        label: "Artigos Publicados"
        file: "public/posts.json"
        fields:
          - label: "Selecione os artigos que aparecerão no blog"
            name: "posts"
            widget: "list"
            summary: "{{fields.titulo}}"
            field:
              name: "slug"
              widget: "relation"
              collection: "posts"
              search_fields: ["title"]
              value_field: "slug"
              display_fields: ["title"]
              multiple: false